// Archivo de ejemplo para datos de prueba
// Puedes poblar este archivo con datos de ejemplo para tu base de datos

module.exports = async function seed(prisma) {
  // Ejemplo:
  // await prisma.user.create({ data: { ... } });
};
